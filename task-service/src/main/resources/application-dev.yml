spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    root:            WARN
    com.taskflow:   DEBUG
    org.hibernate.SQL: DEBUG